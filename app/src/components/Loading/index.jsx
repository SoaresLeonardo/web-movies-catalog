import { LoadingContainer, Title } from "./styled";
import React from "react";

export default function Loading() {
  return (
    <LoadingContainer>
      <Title>Loading...</Title>
    </LoadingContainer>
  );
}
